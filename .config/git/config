[core]
  pager = delta --paging=always || less
[interactive]
  diffFilter = delta --color-only || less
[delta]
  features = decorations unobtrusive-line-numbers
  line-numbers = true
  whitespace-error-style = 22 reverse
[color]
  ui = true
  colorMoved = default
[pull]
  rebase = true
[fetch]
  prune = true
[mergetool "nvim"] 
  cmd = nvim -f -c \"Gdiffsplit!\" \"$MERGED\"
[merge]
  tool = nvim
[mergetool] 
  prompt = true
[filter "spacify"]
  clean = expand --tabs=4 --initial
[alias]
  dots = "!git --git-dir=$HOME/.dtf.git --work-tree=$HOME/"
  add-dots = "!git --git-dir=$HOME/.dtf.git --work-tree=$HOME/ add -f"
  amend = commit --amend
  reuse-msg = commit --reuse-message=ORIG_HEAD
  soft-reset = reset HEAD~1 --soft
  quick-amend = commit --amend --no-edit
  qa = commit --amend --no-edit
  st = status
  ro = rebase origin/master
  smu = submodule update --init --recursive
  potd = push origin HEAD:refs/drafts/master
  potm = push origin HEAD:refs/for/master
[user]
	email = 59826753+kylo252@users.noreply.github.com
	name = kylo252
[gitreview]
	remote = origin
[credential "https://github.com"]
	helper = !gh auth git-credential
